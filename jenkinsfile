pipeline {
    agent any
    stages {
        stage('build image') {
            steps { 
                sh '''
                sudo docker build -t petclinic .
                sudo docker run -d -p 80:8080 petclinic
                '''
            }
        }
    }
}
